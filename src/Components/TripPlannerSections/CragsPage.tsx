import { Box } from "@mui/material"
import CragSelection from "../CragSelection";

const Crags:React.FC = () => {
    return (
        <Box>
            <CragSelection/>
        </Box>
    )
}

export default Crags;